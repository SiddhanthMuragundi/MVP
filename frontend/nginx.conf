server {
  listen ${PORT};
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA history-mode fallback so client routes resolve to index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
